<div
  class='btn clarify-city__btn {{#if top}}clarify-city__btn--top{{/if}}'
  lat='{{lat}}'
  lng='{{lng}}'
  name='{{name}}'
  country-name='{{countryName}}'
  
> <span>{{name}}</span> <span>({{countryName}})</span></div>